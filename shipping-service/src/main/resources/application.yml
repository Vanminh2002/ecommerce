server:
  #  servlet:
  #    context-path: /order-service
  port: 8009


#H2 DB
spring:
  application:
    name: shipping-service
  datasource:
    url: jdbc:postgresql://localhost:5432/shipping_services_db
    username: postgres
    password: manhden123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect