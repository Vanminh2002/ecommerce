server:
  #  servlet:
  #    context-path: /order-service
  port: 8012


#H2 DB
spring:
  application:
    name: role-service
  datasource:
    url: jdbc:postgresql://localhost:5432/role_services_db
    username: postgres
    password: manhden123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect