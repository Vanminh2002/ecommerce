server:
  port: 8005
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/category_services_db
    username: postgres
    password: manhden123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/